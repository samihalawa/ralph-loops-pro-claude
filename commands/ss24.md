---
description: "PRODUCTION REPO POLISHER - Stack Auth + Deploy for AI Studio apps"
argument-hint: "[REPO_PATH]"
---

# SS24 - PRODUCTION REPO POLISHER

Turn a prototype into a shipped product.

## MISSION
Take this repository and make it 100% PRODUCTION-READY, DEPLOYABLE, and PROFESSIONAL. Optimized for AI Studio apps.

## NON-NEGOTIABLES

### 1. AUTHENTICATION (Stack Auth)
- Implement Stack Auth (@stackframe/react) for all auth flows
- Use `VITE_STACK_AUTH_PROJECT_ID` and `VITE_STACK_AUTH_PUBLISHABLE_CLIENT_KEY`
- Ensure `StackHandler` is correctly set up at `/handler/*`
- Wrap app in `StackProvider` + `StackTheme`
- Remove any "Manus" or legacy auth references

### 2. PERSISTENCE & DATA
- Ensure user data persists across sessions
- Bind data to `stackAuth.user.id`
- If no backend, use `localStorage` or `Cloudflare KV` (simplest path)
- Ensure database connections use SSL/TLS

### 3. MARKETING & SEO (The "Front")
- Full SEO: Meta title, description, OG tags, Twitter cards
- Sitemap.xml and robots.txt
- Landing Page: Clear value prop, features, CTA
- Footer: Standard contact info

### 4. UI/UX POLISH
- NO broken buttons or dead links
- Mobile responsive check
- Loading states for all actions
- Error handling (Toasts/Alerts)

### 5. CODE QUALITY
- NO PLACEHOLDERS (e.g., "Add logic here")
- Remove unused files/dead code
- TypeScript strictness (fix `any`)

## EXECUTION STEPS
1. **ANALYZE**: Scan repo for missing SEO, broken auth, or placeholders
2. **IMPLEMENT AUTH**: Install/Configure Stack Auth
3. **FIX UI/UX**: Polish layout, add footer, fix responsiveness
4. **ADD SEO**: Inject meta tags and generate sitemap
5. **VERIFY**: Test login, data save, and mobile view

**REPO TO POLISH**: $ARGUMENTS

EXECUTE NOW.
